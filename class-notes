>>>>> Breaking apart a URL <<<<<

http: = protocol
google.com = host
(http):80 || (https):443 = port > will use default if ommited.
/home etc = path
?q=a = querystring (name-value pairs separated by character to represent space)
#something = fragment

http request methods

>>>>> Show browser header information <<<<<
app.get('/headers', (req, res) => {
    res.set('Content-Type', 'text/plain');
    var s = '';
    for (var name in req.headers) s += ':' + req.header[name];
    res.send(s);
});
